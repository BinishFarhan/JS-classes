// ternary operators. 
let b = 20

// const variable = condition ? true condition: false condition
const a = ( b > 10 ) ? "b is greater than a" :  "b is smaller than a";
console.log("a:", a)

let num = -1
const number = (num < 0) ? `negetive ` : (num === 0) ? `zero` : `positive`
console.log("🚀 ~ file: ternaryOpt.js:10 ~ number:", number)
